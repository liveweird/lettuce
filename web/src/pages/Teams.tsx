import { Stack, Text, Title } from "@mantine/core";

export default function Teams() {
  return (
    <Stack gap="xs">
      <Title order={2}>Teams</Title>
      <Text c="dimmed">Team management lives here.</Text>
    </Stack>
  );
}
