# Frontend (`web/`)

Vite + React 19 + TypeScript SPA. The Gradle and npm toolchains are disjoint — never invoke npm from Gradle or vice versa.

- Dev server: `cd web && npm run dev` (port 5173). All backend routes live under the `/api/` namespace (`/api/v1/login`, `/api/v1/logout`, `/api/v1/users`, `/api/v1/teams`, `/api/v1/feedbacks`, …) and Vite proxies the single `/api` subtree → `http://localhost:8080`. Any other path is served as `index.html` so React Router owns the SPA URL space and browser reloads don't collide with API routes.
- Production build: `cd web && npm run build` → static files in `web/dist`. In the Docker image these are baked in and served by the Ktor server itself (via `WEB_STATIC_DIR`; see "Server bootstrap model" / `plugins/Routing.kt`), so production is single-origin and there is no Vite proxy — the SPA and `/api` share `http://localhost:8080`.
- Regenerate API types: `cd web && npm run gen:api`. Reads `server/src/main/resources/openapi/documentation.yaml` directly (no server needed) and writes `web/src/api/schema.ts`. Run this after editing the OpenAPI spec; commit the regenerated `schema.ts`.
- **Build version stamp**: `vite.config.ts` injects `__APP_COMMIT__` (short sha, `+dirty` when the worktree has uncommitted changes) and `__APP_COMMIT_TIME__` (commit ISO timestamp) via `define`, declared in `src/vite-env.d.ts`. Env vars `GIT_SHA`/`GIT_COMMIT_TIME` override the local-git lookup — the Dockerfile's SPA stage sets them explicitly (its worktree never matches the index, so the dirty check would false-positive), and CI can do the same when building without `.git`. `src/components/VersionStamp.tsx` renders the stamp (`7caf94e · 2026-07-03 14:22`) at the bottom of the navbar (`App.tsx`) and under the login card (`components/AuthCard.tsx`, shared by Login + ResetPassword since v1.35.0); tooltip label is `common.buildInfo`.

The OpenAPI spec at `server/src/main/resources/openapi/documentation.yaml` is the contract between backend and frontend — it is hand-maintained, not auto-generated from routes. When adding/changing a route, update the spec in the same change. Swagger UI is mounted at `http://localhost:8080/openapi`.

The typed fetch wrapper lives in `web/src/api/client.ts` — token storage (localStorage), `login()`, `logout()`, and `authedFetch()` for arbitrary calls. The types come from the generated `schema.ts`; the client itself is hand-written and is the right place to extend when adding new endpoints. Avoid pulling in heavyweight client generators (Orval/Kiota) — the lightweight pairing of `openapi-typescript` (types only) + hand-written fetch is intentional.

`openapi-typescript` is installed with `--legacy-peer-deps` because its declared peer is TS `^5` while the scaffold uses TS 6; the generated output is compatible. If you re-`npm install` from scratch, use `npm install --legacy-peer-deps`.

## Changelog & app versioning

The user-facing changelog is a **build-time artifact** — no DB, no API, changes only with a deploy. `web/src/changelog/entries.ts` holds `ChangelogEntry` rows (`version`, `date` `YYYY-MM-DD`, `en`/`pl` **markdown** bodies), newest first, and exports `APP_VERSION = CHANGELOG[0].version` — the app's only human-readable version. **Adding an entry at the top IS the release/version bump**; there is no other version constant to update (the Gradle `1.0.0-SNAPSHOT` is unrelated). Release convention:

- Write both language bodies by hand (or LLM) — never derive them from commit messages. Bodies are *content*, so they live in the data file, not `locales/`; the PL body follows the Polish style rules below (declined loanword `feedback`, inclusive slash forms, active voice).
- Keep dates descending (same-day releases are fine — newest stays on top), versions unique, and both bodies non-empty — pinned by `web/src/changelog/entries.test.ts`.
- Keep phrases that tests assert on in plain text runs: markdown formatting splits text nodes, and testing-library's `getByText` matches direct text nodes only.

Rendering & UI wiring:

- `pages/Changelog.tsx` (`/changelog`, authenticated, lazy) renders the entries as a Mantine `Timeline` with `MarkdownView` bodies in the viewer's language (EN fallback), and calls `markChangelogSeen()` on mount.
- `components/VersionStamp.tsx` shows `v<APP_VERSION> · <sha> · <time>`; its optional `to` prop renders it as a router link — the navbar instance links to `/changelog`, the Login instance deliberately stays plain (the route is behind auth). A "Changelog" nav item (`CHANGELOG_NAV` in `App.tsx`) is pinned last, directly above the stamp.
- The **"what's new" dot** is a red Mantine `Indicator` around the navbar stamp in `App.tsx`, shown while localStorage `lettuce.changelog` (`{seenVersion}`) differs from `APP_VERSION`. It's driven by `hooks/useChangelogSeen.ts` via `useSyncExternalStore` over a module-level listener set — do **not** replace that with a plain localStorage read in state: the shell renders before the page's mark-seen effect, so the dot would only clear on the next navigation. The seen-state is device-level (survives logout, like `lettuce.lang`) and hand-rolls its guarded read/write (the `AlertsBanner` pattern), not `useStoredState` (that hook owns the `lettuce.viewSettings.*` namespace).
- Tests: `pages/Changelog.test.tsx`, `changelog/entries.test.ts`, the dot/nav cases in `App.test.tsx`, and the link/prefix cases in `components/VersionStamp.test.tsx`.

## Shared list-page building blocks

The list views come in two shapes — **tables** (Users, Teams, Templates, FeedbackTable: long, paginated, filterable) and **person-card grids** (the dashboard's ManagersTable and TeamMembersTable: a handful of people as `PersonCard`s in a responsive `SimpleGrid` rendered as a semantic `<ul>`). Both are built from shared pieces — a new list should reuse them rather than re-declaring the scaffolding:

- **`hooks/usePagedSort.ts`** — the page/pageSize/sortField/sortDir state for a list, exporting `sortParam` (the API's `-field` string), `toggleSort`, and `PAGE_SIZE_OPTIONS`/`DEFAULT_PAGE_SIZE` (20/40/60, default 20). Pass it the (debounced) filter values as `filterDeps`; any filter or sort change resets `page` to 1, as does changing `pageSize`, so the user is never stranded past the last page.
- **List settings persist per view in localStorage** (`lettuce.viewSettings.<viewKey>.*`): filters, sort field/direction, pageSize, and the FilterPanel open state are restored when the user returns to a view; the page *number* is deliberately not persisted. Three pieces, all validated against stale/corrupt stored values: `hooks/useStoredState.ts` (a `useState` replacement — pages use it for each filter atom with an `isString`/`isOneOfOrNull(...)` validator), `usePagedSort`'s optional third arg `{ key, sortFields }` (persists sort+pageSize; `sortFields` is the runtime whitelist — pages now declare `const SORT_FIELDS = [...] as const` and derive their `SortField` type from it), and `FilterPanel`'s required `storageKey` prop (open/collapsed state). View keys in use: `users`, `teams`, `templates`, `alerts`, `myTeams`, `teamMembers.<view>`, `teamSubordinates` (the /teams/:id/subordinates embedding of `TeamMembersTable`, shared across teams), `feedbacks.<view>`, `goals.own/managed`, `userGoals`/`userGoals.managed`, `teamKpis.<view>`/`teamKpis.team`, `performanceReviews.own`, `userReviews`/`userReviews.managed`/`userReviews.audit`, `dashboardReviews` (the Performance page's Team's-performance tab — period + filters + the v1.40.0 `.view` table/chart toggle and `.chartCategory` tab; the key kept its pre-v1.45 Dashboard-era name on purpose so saved settings survived the move), `daysOff.own`/`daysOff.managed`/`userDaysOff.audit`/`userDaysOff.managed` + `daysOff.calendar.scope` (the calendar's member/managed pick — not a list view but the same mechanism), `appShell.navCollapsed` (the desktop navbar's show/hide toggle, v1.34.0 — not a list view but the same persisted-settings mechanism), and `managerFeedbacks.received/provided` (the per-manager screen passes `settingsKey` to `FeedbackTable` so its settings don't bleed into the main tabs — do the same for any new embedding). Settings are device-level (they survive logout, like `lettuce.lang`).
- **`components/PaginationBar.tsx`** — the list footer (total count, page-size select, `Pagination`); renders from `usePagedSort`'s state. **`components/SortHeader.tsx`** — the clickable sortable column header for table views. Card grids have no column headers, so `TeamMembersTable` drives the same `usePagedSort` state from a compact "Sort by" `Select` + direction `ActionIcon` instead (`common.sort.*` keys).
- **`components/ClearableTextInput.tsx`** — the filter `TextInput` with a `CloseButton` in `rightSection` used by the list filter rows (inside the collapsible `components/FilterPanel.tsx`). Form-page clear buttons are a different, `@mantine/form`-bound pattern and stay local to the form.
- **`hooks/useDeleteConfirm.ts` + `components/ConfirmDeleteModal.tsx`** — the delete-confirmation flow for row deletions (target state, open/close, mutation wiring). The modal body is a render prop because each page's confirmation text interpolates differently; the confirm label defaults to Delete but pages whose action reads differently pass `confirmLabel` (the membership screens' "Remove"); page-specific behavior (Users' self-delete logout, per-page cache invalidation) stays at the call site. Non-row confirmations (discard a form, reject/withdraw/delete a feedback) use **`components/ConfirmActionModal.tsx`** instead — message + neutral cancel + red confirm, where the confirm is either a navigation (`confirmTo`, the discard flows) or an action button (`onConfirm` + `loading`). **Every `MarkdownEditor` form (feedback, template, alert — create and edit) guards Cancel with this discard confirm**; a bare cancel link on a long-form editor is a convention violation. Short config forms (user/team/password) deliberately skip it.
- **`utils/saveError.ts` `saveErrorMessage(err, t, keys)`** — the shared mutation-error → message mapper (403/404/409/400 → per-area i18n keys via `SaveErrorKeys`, then `failedStatus` with `{{status}}`, then the `failed` network/generic fallback; optional keys fall through). Pages that map a 409 to a **field** error (`form.setFieldError` — users' email, templates' name) handle that case at the call site and delegate the rest. Don't hand-roll status→message chains.
- **Error `Alert`s** are `color="red" variant="light"` everywhere; list-load failures additionally carry a `title` (form/detail errors render the message bare).
- **Success toasts (v1.38.0)**: every successful mutation confirms itself via `showSuccessToast(t("<area>.toast.*"))` from **`utils/toast.tsx`** — the single writer to the `@mantine/notifications` host mounted in `main.tsx` (`position="top-center"`, `autoClose 2500`, `limit 3`; top-center is deliberate — bottom-right would cover PaginationBar/footer buttons and top-right the header user-menu/bell cluster the e2e helpers click). Success only — **errors stay inline** (the red Alerts above). Toast color is teal (semantic success). **Content rule: fixed vocabulary only, never user-entered names/values in a toast** (interpolated data would break strict-mode `getByText` e2e locators and leak content across screens). Leverage points: `useDeleteConfirm`'s `successMessage` option (list-page deletes), the view screens' `ACTIONS`/`NEXT_ACTION` maps' `successKey` (lifecycle transitions), and the edit pages' `afterSave(successKey)`. Deliberate skips: the notifications bell, auth flows, CreateUser (its generated-password panel IS the confirmation), ImportUsers (per-row result table), and Users' self-delete branch (fires mid-logout). The host lives in main.tsx, not App.tsx, so unit tests never mount it — `notifications.show` is a bare store write in tests; assert it with `vi.spyOn(notifications, "show")` (see `Teams.test.tsx` / `ViewGoal.test.tsx`).
- **Back affordances**: form/detail pages exit via a footer Close/Cancel `Button`; drill-down screens (`ManagerFeedbacks`, `TeamMembers`, `UserTeams`) use a top "Back to …" `Anchor` instead (pattern: `ManagerFeedbacks.tsx`).
- **Row/cell visual language** (keep new lists coherent): `components/PersonCell.tsx` (the shared table person cell — PersonaChip for identifiable other people, plain text for "You"/deleted/absent; GoalTable uses it, the feedback/1:1 tables still carry local copies of the same rule), `components/ProseBox.tsx` (the bordered read-only prose shell — goal description/summary, template content; `minHeightLines` prop; ViewFeedback's tinted focusable content box is deliberately its own), `components/ReadOnlyField.tsx` (input-style label over content at input height, flush with PersonaField — don't re-inline the `Input.Wrapper`+`Box mih=36` pair), `hooks/useDashboardDrillDown.ts` (the `/users/:userId/…` drill-down scaffolding — origin resolution + `backTo` rebuild, including the parameterized `team` origin: `from=team&teamId=` returns to `/teams/:id/subordinates`, degrades to `managers` without a valid teamId, the `details` origin returning to the user-details page, and `auditMode` — `?mode=audit` honored only for `canAudit()` callers (HR-only since v1.26.0; non-auditors, ADMIN included, silently fall back), switching UserGoals/UserOneOnOnes/ManagerFeedbacks to the read-only `view=user` auditor tables (v1.25.0; entry point: the Audit section on `pages/UserDetails.tsx`) — used by UserGoals/UserOneOnOnes; ManagerFeedbacks keeps its wider origin set. **v1.39.0 round-trip params**, mirrored in ManagerFeedbacks' resolver and threaded through the `backTo` rebuild: `back=` (in-app path only) overrides the return destination while the origin key keeps naming the label — UserDetails sends `from=details&back=<its own URL>` on every drill-down so the round-trip preserves the details page's origin — and `manages=1` asserts caller-manages so `from=details` keeps the New-goal/1:1/review affordances; build these URLs only via the link builders' `DrillDownOpts` tail param), **`components/PersonCardActions.tsx`** (the per-person card action/drill-down buttons shared by BOTH dashboard grids, UserDetails' three card flavors, and its HR-audit block — the label table in `components/personCardSupport.ts` picks the `users.*`/`teams.*`/`users.audit.*` wordings, `show` flags pick the buttons, and the `only` prop restricts to one card section's subset (v1.46.0 — `OPERATIONAL_ACTIONS`/`PERFORMANCE_ACTIONS`/`DAYS_OFF_ACTIONS` + the `hasVisibleActions` predicate live in that support file, the tourSupport Fast-Refresh split); don't re-inline card action rows), `utils/goalQueries.ts`/`utils/oneOnOneQueries.ts` (`invalidateGoal`/`invalidateOneOnOne` — the one place a goal/1:1 mutation invalidates from, **including the dashboard card grids** so the Active-goals/Last-1:1 stats stay fresh; don't hand-roll invalidation blocks in pages), `components/PersonaChip.tsx` (initials `Avatar` + truncating name — used in every table person cell), `components/PersonCard.tsx` (the dashboard card: avatar, name, email, team badges, and an optional `body` — `PersonCardBody` in `components/PersonCardStats.tsx` (v1.46.0), which groups everything below the header into **labeled divider sections**, each pairing its stats with its buttons: **Profile** (the career rows, always), **Collaboration** (per the `stats` variant `manager`/`subordinate` → last 1:1 + last feedback + active goals, `peer` → the two feedback directions, `none` → buttons only; plus the provide/ask/request/New-1:1/feedbacks/1:1s/goals buttons), **Performance** (the last authored review's period + status, prop-gated via `showLastReview` — subordinates grid + UserDetails' direct-report flavor only, whose rows actually carry the stat — plus the reviews button), and **Days off** (next vacation for peers and subordinates, budget via `showDaysOff`, plus the days-off button); a section renders only when it has content, stat rows `wrap="wrap"` on purpose so long values fold instead of breaking the card; both dashboard grids cap at **2 cards per row** (v1.34.0)), `components/PersonaField.tsx` (form-header party display — input-style label over a PersonaChip, or plain "You" for the current user; used by Ask/Request/triage instead of disabled inputs), the `components/FeedbackMeta.tsx` people line (provider → subject · requested-by as PersonaChips, same "You"-stays-plain convention), `components/ReportsScopeSelect.tsx` (the shared "Reports" direct-vs-transitive filter — `common.reportsScope.*` keys — used by the subordinates card grid and the team feedback table), `components/FeedbackBadges.tsx` (`StatusBadge`/`VisibilityBadge` — the colored pills, single color source shared with the view/edit header `components/FeedbackMeta.tsx`; in table cells give badges `min-width: max-content` or Mantine silently ellipsizes them), `components/EmptyState.tsx` (dimmed icon + message for empty lists), and `formatRelativeTime` in `utils/datetime.ts` (localized "2 days ago" via `Intl.RelativeTimeFormat`, exact timestamp in the cell `title`). `utils/teamRows.ts` `groupTeamRows` dedupes the per-(user, team) membership rows into one card per person. **The design language is theme-owned (v1.35.0, "clean enterprise SaaS")** — `src/theme.ts` + `src/theme.module.css` are the single source: the brand green is the interactive accent (deep `primaryShade` light 7; buttons/links/active nav/subtle row actions inherit it — **never reintroduce `color="blue"` actions**), SEMANTIC success is **teal** (SENT/ACTIVE/PUBLISHED badges — never stock `green`, which would impersonate the brand), the shell renders white header/navbar surfaces over a tinted canvas (`AppShell.extend` classNames), and **every `Table` is card-framed theme-wide** (surface + hairline border + radius, neutral compact dimmed headers, `highlightOnHover` default — don't add per-table frames or header backgrounds). Soft diffuse `shadows` scale; heading sizes tightened (h2 ≈1.3rem). The logo SVGs (`public/logo-*.svg`, also the favicons) are the geometric leaf mark. Restyle rule: keep aria-labels, roles, real `<table>`/`<code>` elements, and `data-tour` anchors stable. **Form/detail pages use exactly two Container tiers (v1.35.1)**: `size="sm"` for simple field forms (user/team/password config, dictionaries, review periods) and `size="md"` for every content/document screen (feedback, 1:1s, goals, KPIs, reviews, templates, alerts, import, changelog) — a flow's create/edit/view screens share one width; never introduce `xs`/`lg` containers on pages.
- **`pages/FeedbackTable.tsx` is the single feedback table for all three views** (`received`/`provided`/`team`) — there is no separate team table. A per-view `VIEW_CONFIG` (its `personColumns` array drives the filter inputs, sortable headers, and row cells; a per-view `renderAction` supplies the action button) is where view differences go; don't fork the component. Rows render person cells as `PersonaChip`s ("You"/absent/deleted stay plain text), a truncated `contentPreview` column, and the status/visibility pills. The team view's tests live in `FeedbackTable.team.test.tsx`.
- **Feedback flow links are built via `utils/feedbackLinks.ts`** (`feedbackProvideLink`/`feedbackAskLink`/`feedbackRequestLink`/`userFeedbacksLink`) — never hand-assemble `/feedback/*` or `/users/:id/feedbacks` URLs. **Goal links likewise via `utils/goalLinks.ts`** (`goalCreateLink`/`goalViewLink`/`goalEditLink`/`userGoalsLink`) — every `/goals/*` and `/users/:id/goals` URL goes through them, **and user-details links via `utils/userLinks.ts`** (`userDetailsLink`). On the Users and team-members **tables** the per-row Provide/Ask/List-feedbacks actions are grouped behind `components/FeedbackActionsMenu.tsx` (a "Feedback ▾" Mantine `Menu` — the aria pattern is trigger `users.feedbackActionsFor` → `menuitem`s; e2e clicks go through the `clickProvideFeedback`/`clickAskForFeedback` helpers); the dashboard **card grids** deliberately keep inline buttons.
- **`pages/UserDetails.tsx`** (`/users/:userId/details`, v1.21.0) — the read-only, relationship-aware person view: one dashboard `PersonCard`, flavor picked by the first caller-relative `/teams/members` view containing the user (**their-manager > my-direct-report > peer**), with that flavor's stats block and card actions (drill-downs carry the matching dashboard origin; Provide/Ask/New-1:1 return here via `back=`). Resolution is client-side over `listAllTeamMembers` (the paging-loop sibling of `listAllTeams` in `api/client.ts`); a user in none of the views (unrelated, or the viewer themselves) still gets a card from the open users list + `memberId`-filtered teams — without stats (the server never computed them) and, for self, without actions. Entry buttons ("User details", hidden on one's own row/persona) on `/users` (`from=users`), `/teams/:id/members` (`from=members&teamId=` — the back link returns to that roster, degrading to `/users` without a valid teamId), and `/teams` next to each row's manager chip (`from=teams`, non-deleted managers only). The `?name=` param feeds the heading pre-load (the UserTeams idiom — `getUser` is self-or-admin only).
- **`pages/OrgChart.tsx`** (`/org`, Config → Org chart, v1.23.0) — the whole organization as a **React Flow canvas** (`@xyflow/react` + `@dagrejs/dagre` — **lazy-loaded** like MarkdownEditor so they never touch the main bundle; the only other viz dep is `@mantine/charts`/recharts, likewise confined to its two lazy chunks `components/TeamKpiChart.tsx` and `components/ReviewRatingDistribution.tsx` (v1.40.0) — see "Team KPIs" in the root CLAUDE.md). Data is composed client-side (`listAllTeams` + per-team `getTeam` for memberIds + `listAllUsers`); the pure builder/layout lives in `utils/orgGraph.ts` (unique person nodes make the DAG — a member managing another team links onward; dagre `acyclicer: "greedy"` survives cycles; centers→top-left conversion is the documented dagre gotcha; **every** user gets a node — people in no team are edge-less, which routes them out of dagre into wrapped rows below the chart under a "Not in any team" section-label node, v1.30.2). Nodes are our own Mantine components: PersonNode (PersonaChip; plain+dimmed when deleted, plain for self — no self-details) → `userDetailsLink(id, name, "org")` (the `org` back-origin), TeamNode → `/teams/:id/members`; edges are theme-aware via Mantine CSS vars (manages = brand, member = dimmed; legend in the header). Unit tests mock `@xyflow/react` but render the REAL node components through `nodeTypes` (happy-dom can't measure the canvas); i18n area `org`.
- **Testing note:** avatars/badges inside cells become part of the cell's *accessible name* — table tests (unit and e2e) should query by text, not by `cell` role with a name.

## Internationalization (i18n)

The SPA is bilingual (English default + Polish) via **react-i18next** (`web/src/i18n.ts`). All user-facing strings go through `const { t } = useTranslation()` / `<Trans>` — **no hardcoded UI text**. Conventions:

- **Resources** live in `web/src/locales/{en,pl}/<area>.json`, one file per area (`common`, `appShell`, `auth`, `dashboard`, `feedback`, `oneOnOne`, `goals` — mounted as area `goal` — `teamKpis` — mounted as area `teamKpi` — `performanceReviews` — mounted as area `performanceReview` — `daysOff`, `users`, `teams`, `templates`, `dictionaries` — mounted as area `dictionary` — `notifications`, `alerts`, `changelog`, `tour`, `org`); `i18n.ts` merges them into a single `translation` namespace, so keys read `area.key` (e.g. `t("feedback.editTitle")`). Keys are currently **untyped** (no `react-i18next.d.ts` augmentation) — a possible future improvement.
- **`common.*` is the shared source**: actions, field labels, table/pagination, filters, and the enum labels `common.status.*` / `common.visibility.*` / `common.role.*`. Reuse it instead of duplicating; build Mantine `Select` option labels from `t()` at render so they translate.
- **Creation-verb convention (v1.15.2):** buttons that open a create screen say **"New <thing>"** / PL **"Nowy/Nowa/Nowe <thing>"** (New team, New user, New template, New feedback, New alert, New 1:1, New goal, New team KPI — and the create screens' titles reuse the same wording); in-form list adders say **"Add …"** / **"Dodaj …"** (Add point, Add a provider, Add to team); the create form's submit button is the bare `common.action.create` **"Create"** / **"Utwórz"**. New buttons must pick the tier that matches their behavior, not mix verbs.
- **Keep EN/PL key parity** — every English key must exist in Polish (Polish-only plural variants like `_few`/`_many` are expected). Use i18next interpolation (`{{name}}`) and plural keys (`_one/_few/_many/_other`) rather than string concatenation.
- **Polish term for "feedback" is the loanword `feedback`, declined** (feedback / feedbacku / feedbacki / feedbacków), *not* "opinia". `teams.json` is the style reference.
- **Polish voice/gender convention:** **inclusive slash forms**, active/direct voice, same tense and meaning as the English — 3rd person `"{{provider}} odmówił/a…"`, 2nd person `"Poprosiłeś/aś…"`, participles `"wylogowany/a"`. Spell out irregular feminines in full (e.g. `"usunął/usunęła"`, never `"usunął/a"`). Do **not** use impersonal/passive dodges (`"Poproszono…"`, `"Wylogowano Cię"`).
- **Tests** render English: `web/src/test/setup.ts` imports `../i18n` and forces `en`, and English keys resolve byte-identically to the old literals, so text-based assertions keep working. Every test `MantineProvider` (the shared `src/test/render.tsx` wrapper and each file-local one) must pass **`env="test"`** — since Mantine 9.4 the Popover/Combobox dropdown is `display: none` until Floating UI's `hide` middleware sees a real bounding box, which never happens in happy-dom, so Select-option clicks silently fail without it. The language switcher is `web/src/components/LanguageSwitcher.tsx` (header); choice persists in `localStorage` (`lettuce.lang`) and updates `<html lang>`.
- **Scope:** frontend chrome is translated, and both **feedback history events** and **notifications** are localized client-side — the server stores them structurally (`type` + `params`), with no rendered string (see "Feedback events" / "Notifications" in the root CLAUDE.md). No server-generated user-facing text remains English.
