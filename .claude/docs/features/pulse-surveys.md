# Pulse surveys (v2.0.0)

Recurring anonymous eNPS surveys: admin-managed cycles, per-user encrypted responses, team-aggregated results with k-anonymity, manager participation monitoring, anonymized comments. Server package `pulse/` (+ `settings/AppSettingsService.kt`, `teams/TeamTree.kt`); SPA `/pulse` hub + `/pulse-cycles` admin screen; feature flag `PULSE_SURVEYS`; dictionary `pulse-rotating-questions`.

## Cycle machine (`pulse_cycles`, V48)

`SCHEDULED → OPEN → CLOSED`, terminal `CANCELLED` reachable from ANY of the three (cancelling a CLOSED cycle retracts its results; responses are always kept for audit). **Every transition is a manual ADMIN action** — `planned_open_date`/`planned_close_date` are advisory prefills; the app has zero background jobs and none may act on them. **At most one non-terminal (SCHEDULED/OPEN) cycle exists at a time**: service pre-check → 409, race-proof backstop via V48's partial unique index over a constant (`uq_pulse_cycles_non_terminal`). PUT date edits: SCHEDULED = both dates; OPEN = close date only ("extend" — a changed open date is 400); CLOSED/CANCELLED = 409. Soft-delete column per convention but **no DELETE endpoint** (cancel is the removal — the `days_off_requests` precedent).

## Eligibility snapshot (`pulse_participants`, V49)

Taken at OPEN: active (not soft-deleted) ∧ not deactivated ∧ PULSE_SURVEYS enabled → participant rows. Later churn never changes a cycle's history; a user enabled after open is NOT a participant (403 on my-response). Response rates and participation monitoring read the snapshot. The schedule-time notification audience is computed the same way but deliberately un-stored (may drift from the later snapshot — documented).

## Survey content & responses (`pulse_responses`, V49)

Q1 eNPS 0–10; Q2–Q5 fixed statements (client-side i18n, NOT stored); Q6 rotating question; Q7 optional comment ≤1000 chars. Answers stored as strings (`"0".."10"` / `"1".."5"` / `"NA"`), **all six scored columns + the comment encrypted** (FieldCipher envelopes — the V45 review-ratings precedent; `encryptLegacyRows()` hook in Bootstrap; never filter/sort these columns in SQL). One row per (cycle, user) — UNIQUE + FK into participants; **upsert while OPEN** (`submitted_at` set once, immutable). `GET/PUT …/my-response` is the ONLY endpoint ever serving individual answers — own answers only, and **only while OPEN** (after close: 409 — the anti-copy-paste rule). `PulseResponseService.answersForScope` returns the user-id-free `PulseAnswers` type, so no aggregate can leak an author by construction; comments come back author-free and **shuffled per request**.

## Rotating question (Q6)

Fourth dictionary `PULSE_ROTATING_QUESTION("pulse-rotating-questions")`, seeded by V50 with the six bank statements; admin-editable via the generic dictionary machinery (**deliberately ungated** server-side like all dictionaries — the nav leaf is feature-tagged as pure UI). Pick at SCHEDULE time (`PulseCycleService.pickRotatingQuestion`, injectable `Random`): candidates = ACTIVE entries with the LOWEST usage count over non-cancelled cycles, uniform random among ties — pool-without-replacement with an implicit reset when counts equalize; a cancelled cycle returns its question; a newly added entry (usage 0) is automatically next; an empty bank refuses to schedule (409). The entry's **text is snapshotted** onto the cycle (`rotating_question_text_en`/`rotating_question_text_pl`) — renames never rewrite what a cycle asked. Rotating-question deltas compare only when the previous cycle used the SAME entry id.

## Team scopes (`teams/TeamTree.kt`)

The team hierarchy is derived, not stored: **team T2 is below T1 iff T2's manager ∈ members(T1)** (set-at-a-time BFS, cycle-safe, non-deleted teams, ManagementChain style; wrapped for routes by `TeamService.visibleTeamTreeIds` / `managedTeamTreeIds` / `teamScopeMembers`).

- `visibleResultTeamIds(user)` = member-of + managed + everything below either — the RESULTS scope.
- `monitoredTeamIds(user)` = managed + below — the MONITORING scope (participation + comments).
- `teamScopeUserIds(team, mode)`: `direct` = current members; `subtree` = members + every transitive subordinate of a member. A manager is never a plain member of their own team — their response aggregates under the team they are a MEMBER of.
- `GET /pulse-surveys/visible-teams` serves both lists to the SPA's pickers (HR: all teams).

## Access matrix (who sees what)

| View | Rule |
|---|---|
| Fill/edit own survey | participant + cycle OPEN (upsert) |
| Team aggregates (`…/results`) | cycle CLOSED (409 otherwise — **uniform, HR included**; CANCELLED never serves results) + **the per-cycle fill gate**: caller responded in THIS cycle (no role exemption — ADMIN included) + team ∈ visible tree. **HR is exempt** from the fill gate (auditor precedent, `hr.read` audited, resource `pulseResults`) |
| eNPS trend (`…/trend`) | team access as results; the fill gate applies **point-wise** for non-HR (`NOT_A_RESPONDENT` = no numbers at all) |
| Multi-scope trend (`…/trend-comparison`, v2.11.0) | exactly the trend rule, checked on the PARENT team only (∈ visible tree — sufficient because visibility is transitive, so every series is a team the caller could query via `…/trend` alone); HR exempt from nothing extra, audited `pulseTrendComparison` (resourceId = teamId, the trend precedent). **No request-level fill gate**: the per-cycle gate applies **point-wise and uniformly across every series** — a total non-respondent gets 200 with `NOT_A_RESPONDENT` everywhere. Payload: `[own direct, own subtree, one subtree series per direct child (name-asc)]`, each a self-describing `PulseTrendResponse` (a series is identified by its (teamId, mode) pair — the parent appears twice); the subtree series is ALWAYS present — for a childless team the handler reuses the direct points at compute level (scope sets equal → zero extra decrypt passes), never dropping the series from the shape |
| Anonymized comments (`…/comments`) | cycle CLOSED + team ∈ MONITORED tree (managers+ only — members never; **no own-fill requirement**); HR org-wide, audited `pulseComments` |
| Per-person participation (`…/participation-status`) | cycle OPEN or CLOSED (live monitoring is the point); monitored tree; bare submitted yes/no, never content; HR org-wide, audited `pulseParticipation`; non-managers get an empty list |
| ADMIN | cycle CRUD + settings + per-cycle **counts only** (`…/participation`: participants/responses/rate) — no aggregates, no comments, no per-user status (the v1.26.0 narrowed-ADMIN rule) |

History note: the v2.10.0 per-cycle **team-comparison** view + `…/cycles/{id}/team-comparison` endpoint (+ its `requirePulseComparisonAccess` guard and `pulseComparison` audit resource) were **removed in v2.12.0** — superseded by the two-view Results layout (member/managed + the direct/indirect calculation toggle) and the Trend tab.

**k-anonymity (k=3)**: a scope under 3 responses withholds aggregates AND comments — `insufficientResponses: true` with null/empty payload (a 200 marker, not an error) — applied to the current scope, the previous-cycle scope (no deltas against a withheld baseline), and each trend point. Existence/ordering idiom: cycle reads are read-before-guard (404 visible); results order deliberately **404 → 409-state → 403-identity** so a non-closed cycle answers uniformly for every caller.

## Aggregation (`PulseAggregation.kt`, pure)

Bands: promoters 9–10, passives 7–8, detractors 0–6. `eNPS = round((promoters − detractors) / n × 100)` — one expression since v2.6.2 (mathematically = round(promoterPct − detractorPct), but drift-free; ties round half-toward-+∞, so 12.5 → 13 and −12.5 → −12) (−100..+100); display percentages 1dp. Per driver: NA excluded everywhere; mean 1dp, favorable = 4–5, unfavorable = 1–2, `validCount` always shipped. `responseRate` = responses/participants in scope, 1dp. Change-vs-previous = the immediately preceding non-cancelled CLOSED cycle (max `closed_at`), same scope: eNPS delta (whole points), per-driver mean Δ + favorable Δpp (1dp, computed over the ROUNDED values). **No result is ever framed as statistically significant** (copy rule too — no i18n string may say "significant").

## Settings (`app_settings`, V47)

The app's FIRST runtime-editable config: generic K/V store (`settings/AppSettingsService.kt`, pulse-agnostic), pulse keys `pulse.cadenceWeeks` (default 4) / `pulse.openDays` (default 7), validated 1–52 / 1–90. **Advisory only** — they prefill the admin scheduling form (`open = latest non-cancelled cycle's open + cadence weeks`, `close = open + openDays`); the server never acts on them. `GET/PUT /pulse-surveys/settings` are ADMIN both ways (the alerts reads-included posture); PUT audited with deltas.

## Notifications (all → `Feature.PULSE_SURVEYS`)

| Type | When → recipients | Link |
|---|---|---|
| `PULSE_CYCLE_SCHEDULED` | schedule → all currently eligible | none (no survey yet) |
| `PULSE_CYCLE_OPENED` | open → participants | `/pulse?tab=survey` |
| `PULSE_RESULTS_AVAILABLE` | close → **respondents only** | `/pulse?tab=results&cycle={id}` |
| `PULSE_CYCLE_CANCELLED` | cancel **from OPEN only** → participants | none |

Pure builders in `PulseNotifications.kt`; actor excluded; params = raw ISO dates (+ cycleId); persisted via the batch `NotificationService.createAll`. Full app-wide table in `.claude/docs/features/notifications.md`.

## Audit events

`pulse_cycle.scheduled` (dates + rotatingQuestionEntryId) / `.updated` (date deltas when changed) / `.opened` (participantCount) / `.closed` (responseCount) / `.cancelled` (fromStatus); `pulse_settings.updated` (deltas); `hr.read` resources `pulseResults`/`pulseComments`/`pulseParticipation` (resourceId = cycleId + `teamId` field) and `pulseTrend`/`pulseTrendComparison` (resourceId = teamId — they span cycles). (`pulseComparison` existed v2.10.0–v2.12.0 only; historical rows may carry it.) No `pulse_cycle_events` table (the admin-registry idiom); response submission deliberately un-audited (user content — the row's user_id + timestamps are the audit record).

## API shape notes

All under `/api/v1/pulse-surveys/*` (tag `pulse-surveys`, 18 operations); every handler resolves `pulseCaller()` FIRST (uniform 403 for a disabled caller). The cycle list is an **unpaged registry** (`{items}`, newest first — a cadence of weeks caps volume; deliberate deviation from the paged-list convention, like review-periods). `rotatingQuestionEn`/`rotatingQuestionPl` on cycle reads (and the results-side pair `rotatingTextEn`/`rotatingTextPl` on the ROTATING driver row) is null for non-admins until OPEN/CLOSED (unopened questions stay unspoiled; CANCELLED reveals nothing to non-admins); the list's `participantCount`/`responseCount` are ADMIN-only enrichment. `DashboardSummary` gained `pulseOpenCloseDate`/`pulseSubmitted` (null unless the caller is a participant of an OPEN cycle) backing the SPA tile.

## SPA map

`pages/Pulse.tsx` (hub `/pulse`, `?tab=survey|results|trend|participation` — participation for managers/HR, the rest for everyone), `PulseSurvey.tsx` (a WIZARD since v2.0.1, reworked v2.5.9: one question per step 0–6, **explicit Back/Next traversal — answering never auto-advances** (the v2.0.1 250ms auto-advance was removed as disorienting; Next is the primary filled button), per-step Next gating = the "all six required" rule, submit only on the comment step; **an existing response parks the tab on a saved-summary screen** (the teal `pulse.editableUntil` notice + the `pulse.action.editAgain` button) — the wizard shows only for a first fill or after "Edit my answers", and a successful save returns to that summary (prefill intact); progress shows both "Question X of 7" and "N of 6 answered". Chip.Group eNPS 0–10 — chips, not SegmentedControl, so the unanswered state shows NO selection, chip row + end-caption row share a `fit-content` box so the captions align under 0/10; `PulseScaleInput` Radio.Groups, options stacked vertically left-aligned (v2.5.9 — a horizontal radio row read too much like the color swatches), NA last; band-dependent comment prompt via `commentPromptKey`. **Favourability colors** (v2.0.1, the reviewRatings ramp re-cut): `pulseScaleColor` 1→orange.8 … 5→green.8 / NA gray.5 as radio color + a ColorSwatch beside each label, `enpsColor` 0–6 orange shades / 7–8 yellow-lime / 9–10 greens tinting the chips — both in `utils/pulseSurveyForm.ts` with `mantineColorVar` for CSS-var rendering), `PulseResults.tsx` (closed-cycle Select + `?cycle=` deep link; **the two-view layout, v2.12.0**: a view SegmentedControl persisted at `lettuce.viewSettings.pulse.results.view` — "Teams I belong to" (`memberTeams`, always wire-direct numbers) vs "Teams I manage" (`monitoredTeams`, plus a calculation SegmentedControl persisted at `lettuce.viewSettings.pulse.results.calc`: "Direct members only" ↔ "Including everyone below" = wire `mode=direct|subtree`) — which teams are LISTED follows the view, not just how each card aggregates (the pre-v2.12.0 `pulse.results.mode` key incl. the removed `compare` value is abandoned in localStorage, harmless); one `PulseTeamResultCard` per team of the active view; **the per-view empty states render BELOW the selector** (a member-of-nothing manager must still reach the managed view); the fill gate is probed once on the active view's first team and rendered as a single informational empty state — but a gated MONITOR (a manager/HR who didn't respond) still gets `commentsOnly` cards for their monitored teams regardless of view (the card skips the aggregate/trend queries and renders only the comments section their monitoring right covers); `canMonitor` stays `isHr() || monitored` per card, so comments render on monitored cards in either view), `PulseTrend.tsx` (the v2.11.0 Trend tab, **two-view since v2.13.0 mirroring the Results tab**: a view SegmentedControl persisted at `lettuce.viewSettings.pulse.trend.view` (per-tab memory — deliberately NOT shared with `pulse.results.view`) over the shared `pulse.view.*` label/empty-state block both tabs use; **member view** = team Select over `memberTeams`, ONE own-members line via the cheap single-team `/trend?mode=direct` (query key `["pulseTrend", teamId, "direct"]` — deliberately identical to the result cards' mini-chart, cache shared), no chips/noChildren; **managed view** = team Select over `monitoredTeams`, the `…/trend-comparison` bundle with per-series toggle chips (all on, reset per team via `key={teamId}`, own-scope labels from `pulse.trend.series.own/subtree`, children by name; own-direct keeps `lettuce.6`, the rest cycle a fixed hue palette); metric SegmentedControl — eNPS or a fixed driver's favorable% — persisted at `lettuce.viewSettings.pulse.trend.metric` and applying to both views (the rotating question deliberately has NO trend metric — it changes between cycles); rows built by `buildTrendComparisonRows` — undefined cells = line gaps (withheld / not-responded / all-NA driver), `n_<seriesKey>` count columns ride the tooltip; fewer than 2 renderable rows → the `trendPending` idiom, which also covers total non-respondents — NO 403 probe, the endpoints never fill-gate a visible team at request level; a childless bundle (2 series) shows the `pulse.trend.noChildren` note; per-view empty states BELOW the selector, the v2.12.0 rule), `PulseParticipation.tsx`, `PulseCycles.tsx` (admin; derived — not effect-set — date prefills). `PulseTrendChart` is the third lazy `@mantine/charts` chunk — generalized in v2.11.0 to `{data, series[], yDomain?}` (multi-series rows, `connectNulls={false}` gaps, allowlist tooltip filter with per-series `(n=…)` labels) so the Trend tab reuses it and the lazy-consumer count stays three. Utils: `pulseSurveyForm.ts`, `pulseResults.ts`, `pulseQueries.ts` (`invalidatePulse` — the single invalidation seam; `pulseLinks.ts` was dead code deleted in v2.5.6). Person cards/`FEATURE_OF` deliberately untouched — a per-person pulse surface would contradict the anonymity model. Tour (v2.1.1 — the three `pulse-*` tab anchors had been rendered but unused since v2.0.0): `nav-pulse` + `pulse-survey`/`pulse-results` + `pulse-participation`, the last carrying the **`managerOrHr`** gate that mirrors the tab's own `canMonitor = isManager || isHr()` (`managerOnly` would have hidden it from a non-managing HR auditor), plus the ungated `pulse-trend` step (v2.11.0) and the adminOnly `config-pulse-cycles` step on the admin page's title.

## Accepted limitations (documented, not bugs)

- `subtree − direct` differencing can isolate a sub-scope under k — accepted for this app's trust model. The managed view's calculation toggle (v2.12.0) presents both scopes deliberately: each is an already-authorized single-team query, independently k-withheld, so switching the toggle discloses nothing differencing could not already derive.
- The previous-cycle delta indirectly reveals a prior aggregate to a current-cycle respondent (the delta is part of the current view).
- Style-based comment attribution is possible at k=3 with visible participation — inherent to the k=3 floor.
- The trend recomputes (and decrypts) every closed cycle per request — fine at this scale; cap later if needed.
- One trend-comparison request (v2.11.0) is the product of the two costs above: (2 + C) scopes × N closed cycles of `answersForScope` decrypt+aggregate passes (one request per team pick, not per visible team). The per-(scope, cycle) queries are forced by the never-pair-a-user-with-an-answer invariant — `PulseAnswers` is user-id-free, so a subtree's answers cannot be partitioned into child scopes after the fact. The childless-team dedup (subtree scope == direct scope → reuse the points) removes the only always-redundant pass; the "cap later" remedy applies to both trend endpoints at once because they share `trendPoints`.
- Q1 says "this company" (no organization-name setting exists).

## Test map

Server: `PulseCycleRoutesTest` (machine, 409s, date rules, notifications, eligibility, dashboard tile, audit), `PulseResponseTest` (upsert, gates, validation), `PulseResultsTest` (access matrix, k, deltas, subtree, comments, participation-status, trend, visible-teams; v2.6.2: route-level eNPS-0 thirds + the equal-closedAt previous-cycle tie → no baseline; v2.11.0: the trend-comparison trio — roster/ordering/per-scope-points/driver-favorability (incl. the all-NA-at-OK null), point-wise-fill-gate/HR-audit/ancestor-visibility, and the childless value-equal subtree series, plus favorable fields on the `/trend` test; v2.12.0: the visible-teams test pins all THREE scopes (member/monitored/results — a manager is NOT a member of the team they manage; the v2.10.0 team-comparison test pair was removed with its endpoint); the trend-comparison guard is route-tested only, like its siblings — no pure GuardsTest case), `PulseAggregationTest` (pure; v2.11.0 `driverFavorablePct` — NA out of the denominator, all-NA null, 1dp thirds; v2.6.2 hardening: ±12.5 ties + the n=24 ex-drift case, negative deltas, mean 3.25 half-up + favorable thirds, all-NA through buildTeamResults suppressing deltas both directions, zero-responses/zero-participants shapes) + `PulseNotificationsTest` (pure), `PulseRotatingQuestionTest`, `TeamTreeTest`, `PulseEncryptionTest`, `PulseSettingsTest`; FeatureFlagsTest gained the pulse paths + type-mapping branch. **Shared-state rules for tests**: the one-non-terminal invariant is global — sweep first (`TestPulse.sweepNonTerminal`) and leave the registry terminal; multi-cycle timelines anchor `closedAt` via `TestPulse.closedAtAfterAll()` (an earlier test's future-skewed closedAt would hijack the previous-cycle lookup); results fixtures use `TestPulse.closedCycleWith` (forced statuses — no whole-container snapshots). Web: per-page/component/util tests (`PulseSurvey.test` incl. the band-dependent prompt; note Mantine Chip in single-select renders `role="radio"`; `PulseTrend.test` — the two views (member single-line via a REAL `/trend` mock route, managed chips/gaps preset via `lettuce.viewSettings.pulse.trend.view`), view switch/persist, metric switch/persist, chip toggles, pending/childless and both per-view empty states — mind the mock-router rule: match `"/trend-comparison"` BEFORE the bare `"/trend"` route, the `/team-comparison`-before-`/results` bug class). e2e: `pulse.spec.ts` exclusively owns the global cycle singleton; since v2.6.2 the results step asserts HAND-COMPUTED aggregates (eNPS 0 + 33.3-thirds, per-row means/favorables/n incl. the NA-shrunk Q5) — deltas deliberately unasserted (rerun-dependent); the v2.11.0 Trend leg asserts chips/notes plus chart-or-pending either-or locators only (chart presence is run-dependent — CI has one closed cycle, shared-DB reruns accumulate; since v2.13.0 it first pins the member empty state, then switches to "Teams I manage"); the v2.12.0 two-view leg covers the member empty state, the managed card set, the calc toggle's widening (CCC "0 of 2" direct → "3 of N responded" subtree — N deliberately unpinned: reviews.spec's Manager-AAA-managed team hangs under CCC in the tree and accumulates per shared-DB run, while the response count and eNPS 0 stay stable), and comments visible in the managed view (the monitoring right — the removed comparison view was the only comment-free surface). The 2026-08-11 methodology review's future-work note (driver-favorability trends over cycles) SHIPPED in v2.11.0 as the Trend tab / `…/trend-comparison`. Audit-accepted behaviors (2026-08-11): a corrupt/undecryptable response row fails the whole scope's read loudly (500, no skip); previous-cycle deltas are computed over CURRENT team membership (one scope resolution); the SPA interpolates `responseRate` raw ("75%" beside "66.7%"). v2.6.2 context additions: the driver table gained the Δ favorable column (`pulse.results.favorableDelta` — the wire's `favorableDeltaPp`, finally rendered), the card shows a volatility hint below `PULSE_SMALL_SAMPLE = 10` responses, and the eNPS headline carries an approximate band color/caption (`enpsBandKey/Color` in `utils/pulseResults.ts`: <0 concern/red, 0–20 okay/dimmed, 21–50 good/teal, >50 excellent/teal — industry convention, not science). v2.6.4: the eNPS headline and all six metric headers of the driver table carry methodology hint tooltips (`HintIcon` in `PulseTeamResultCard.tsx` — the DashboardHero info-icon idiom; `pulse.results.hint.*` keys; the tooltip text doubles as the icon's aria-label).
