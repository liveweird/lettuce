### Dictionaries (global ordered value lists)

> v2.0.0 added a FOURTH dictionary: `PULSE_ROTATING_QUESTION("pulse-rotating-questions")` — the pulse survey's Q6 bank (V50 seed). Same generic machinery; entries must read as statements answerable on the five-point agreement scale; cycles snapshot the picked entry's text (both languages since V53), so renames never rewrite a past cycle (see "Pulse surveys" in `.claude/docs/features/pulse-surveys.md`).

Three **global dictionaries** (v1.31.0) — **career paths**, **career specializations**, **seniority levels** — each an admin-curated **ordered** list of short text values with hidden stable ids — **bilingual since V53 (v2.6.0): every entry holds an English AND a Polish value** (`value_en`/`value_pl`), unique per language within the dictionary, and clients render the viewer's language. One shared table `dictionary_entries` (`V31`): enum-name discriminator column (`Dictionary` in `dictionaries/Dictionary.kt` is the whitelist — slugs `career-paths`/`career-specializations`/`seniority-levels` are its URL forms), `position` (rewritten from payload order on every save; soft-deleted rows keep stale positions — reads filter active), `value_en`/`value_pl` ≤100 each (stored **trimmed**; plaintext), standard soft-delete, and the per-language partial unique index pair freeing a deleted entry's values (see "Soft delete" in `.claude/docs/persistence.md`). Implementation in `server/src/main/kotlin/dictionaries/` (`Dictionary.kt` DTOs + shared `validateDictionaryUpdate`, `DictionaryService.kt`, `DictionaryRoutes.kt`). Defaults are seeded by `V32` (v1.31.1) — additive and idempotent (`ON CONFLICT` targeting the then-single partial unique index; the seeds run before the V53 rename, so their checksums are untouched), so admin-curated values are never overwritten; `V53` backfills Polish (real translations for still-unrenamed seed values — seniority levels/language names keep the market-standard English loanwords — and copy-EN for everything else).

- **API — two operations only** (`/api/v1/dictionaries/{dictionary}`; unknown slug → 404): `GET` (any authenticated) returns the **unpaged** ordered `{items:[{id,valueEn,valuePl}]}` (`position ASC, id ASC` — the `/alerts/visible` shape; deliberately NOT a standard list endpoint, ≤200 entries by validation). `PUT` (ADMIN only, guard before slug resolution, `204`) is a **whole-document replace** — the 1:1 `replaceNotes`/`requirePayloadIds` idiom adapted to soft-delete: an item with an `id` renames/repositions that active entry in place (rename keeps identity), an id-less item inserts, an active entry missing from the payload is **soft-deleted**. The active-rows-only snapshot makes a soft-deleted id a foreign id (400 — no resurrection; re-adding the value mints a new id), and **soft-deletes run before upserts** so remove+re-add and rename-onto-a-removed-value work in one save. Validation (400): blank/oversized values in either language, payload-duplicate values **per language** (case-sensitive, trimmed — the same string may be one row's English and another's Polish), duplicate/foreign ids, >200 items; a clash with a surviving active value (e.g. **swapping two values in one save** — per-statement unique index, documented limitation; workaround is a two-save rename) → 23505 → 409. Empty `items` clears the dictionary. Audit: one `dictionary.updated` per successful save. No events table, no notifications, no encryption (short public-ish labels).
- **SPA**: left-menu group **"Dictionaries"** (visible to everyone; **one group-level tour step** `nav-dictionaries` since v2.1.1 — the four leaves share an editor, so a step each would repeat) with four leaves → the single page `web/src/pages/Dictionary.tsx` at `/dictionaries/:slug` (unknown slug redirects home). Non-admins get the read-only numbered list; an ADMIN gets the **whole-list editor** (the 1:1 draft idiom via `utils/dictionaryForm.ts` — drafts `{key, id?, valueEn, valuePl}`, `toUpdateBody` preserves ids; each row edits BOTH languages side by side (aria `dictionary.entryAriaEn/Pl`; visible "English"/"Polish" column headers since v2.6.1 — a header row mirroring the entry-row structure, incl. an invisible RowControls clone for width), the read-only view leads with the viewer's language and dims the other, and `pickDictionaryValue(entry, lang)` in `utils/dictionaryForm.ts` is the ONE localization seam every display site uses; rows reuse `RowControls` from `components/ParagraphListEditor.tsx`) with one Save that PUTs the document and **re-seeds from the server** so new rows gain their minted ids; dirty Cancel goes through the `ConfirmActionModal` discard guard; client-side duplicate validation mirrors the server, a racing 409 maps to the conflict message. The editor is keyed by slug so switching leaves remounts it. i18n area `dictionary` (files `locales/{en,pl}/dictionaries.json` — the goals filename≠area precedent).
- **Tests**: `DictionaryTest` (authz incl. the uniform-403 probe, replace semantics, id/value validation, soft-delete reuse + raw-row assert via `TestDictionaries`, one-save swap 409, case sensitivity), the `dictionary.updated` case in `AuditTest`; SPA `Dictionary.test.tsx` + `dictionaryForm.test.ts` + the nav cases in `App.test.tsx`; `e2e/tests/dictionaries.spec.ts`.
- **Career profile (v1.32.0)** — every user carries three optional dictionary-backed properties: career path, career specialization, seniority level (`users.career_*_id`, `V33`), stored as **entry ids** and resolved to values at read time, so a dictionary rename propagates immediately and a **soft-deleted referenced entry keeps resolving** to its retained values. `UserResponse`/`UserCreateResponse` carry them as nullable resolved `{id, valueEn, valuePl}` objects (the SPA picks the viewer's language via `pickDictionaryValue`) (`toResponse(id, entries)`; `list` batch-resolves per page, the `rolesByUserIds` idiom; `resolveEntryRefs`/`requireActiveEntries` in `UserService` — the latter 400s unless a **newly-assigned** id is an ACTIVE entry of the matching dictionary, resubmitting the current id is never validated). On PUT/POST the ids are nullable with **null/omitted = leave unchanged** (create: unset) — clearing a set value is deliberately inexpressible; assigning/changing is ADMIN-only (`requireCanAssignProfileFields`, guarded before validation like roles). Audit: `user.updated` id deltas + `user.created` ids (see "Audit trail" in `.claude/docs/observability.md`). SPA: pickers on Create/Edit user (`components/CareerProfileSelect.tsx` over `hooks/useDictionaryOptions.ts` — active options + the stale current entry appended; not clearable; orange missing hint) and, since v1.32.1, the read-only career rows **inside every person card** (details page + the three Dashboard grids): `/teams/members` rows carry the resolved triple on every view (`UserService.careerProfilesByUserIds`, composed route-side like the stats), rendered by `PersonCardBody` as the card's **Profile section** (v1.46.0 — the labeled-divider sections replaced the old two-column stats/career split; every flavor gets Profile, including cards without relationship stats — details self/unrelated, the subordinates grid at reports-scope "all"). `CareerRows` stays caption-less beyond the section divider, with short card-only labels — `users.profile.path/specialization/seniority`; the form pickers keep the full `common.field.*` wordings — one orange "Not set" badge per missing value. Tests: the career sections in `UserRoutesTest`/`AuthorizationTest`/`AuditTest` + the view sweep in `TeamMemberListTest`; SPA `EditUser`/`CreateUser`/`UserDetails`/`ManagersTable`/`TeamMembersTable` tests; the career-profile scenario in `e2e/tests/user-edit.spec.ts`.

